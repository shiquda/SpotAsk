# Changelog

SpotAsk 的所有重要变更都记录在本文件中。

本文件的格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

## [Unreleased]

### Added

- 支持通过 `spotask://settings/<page>` 链接直接打开指定的设置页面（服务设置、提示词、外部提问、划词助手、快捷键、通用、外观、关于），便于文档、快捷指令与终端脚本直达对应页面。基础的 `spotask://settings` 仍可打开设置窗口，未知页面会回退至设置窗口。
- 包含较多行为配置的设置分组现已提供对应的指南链接（服务地址、模型、提示词、外部提问、划词助手、快捷键参考、代理、本地数据、外观）。链接跟随界面语言，自动打开英文或简体中文页面。
- 文档站为服务配置、地址类型、模型选择、外部提问和划词助手绘制了流程图，并在英文与简体中文页面中新增**在 SpotAsk 中打开设置**按钮以直达对应设置页面。每个按钮同时注明了手动操作路径，未安装应用时仍可按指引操作。
- 标题栏的快捷模型选择器现已在「外部提问」分组下展示已启用的外部提问目标。选择任一目标即可带上当前草稿在对应平台中打开；草稿为空时则带上最新问题，且当前对话内容保持不变。

### Changed

- 标题栏模型选择器现居中显示在标题栏内，不再紧贴 SpotAsk 品牌标识；生成中的加载动画保持与选择器归组显示。

## [0.2.4] - 2026-09-16

### Added

- 检查更新现在可直接在应用内下载并安装。你可以在更新提示窗口中选择跳过某个版本，并在「关于」中恢复提醒。若应用内更新无法执行，GitHub Releases 仍可作为备用渠道。
- 可在「设置 > 关于 > 更新」中配置更新下载源（自动、官方 GitHub、国内加速源），并具备自动超时检测与镜像回退机制，确保在不同网络环境下都能可靠下载。
- 在输入框中键入 `@` 可筛选提示词预设与外部提问目标。选择提示词预设会保留草稿；输入框已有内容时选择外部提问目标会立即触发。
- 划词助手动作栏新增专属对话图标，可一键将选中文字填入提问窗口输入框用于追问；该项可在设置中单独开关（默认开启）。
- 在已有对话中，预设弹出菜单现在会在提示词预设旁一并列出已启用的外部提问目标，并从底部输入栏向上弹出；外部提问快捷键会挂载一个待发送标签，在发送时触发。

### Changed

- 选中的提示词或外部提问标签现在位于输入框左上方、输入区域之外，宽度紧凑并自适应内容，因此多余的底部行及其残留空白已移除。
- 输入框为空时，外部提问按钮及其快捷键改为选中一个待发送目标，而不再立即触发；按 Return 发送草稿，再次点击、按 Esc 或清空输入则取消选择。

### Fixed

- 输入框已有内容时，按下外部提问快捷键或从弹出菜单中点选会立即发送，不再需要额外按一次 Return。

## [0.2.3] - 2026-09-11

### Changed

- 设置现在使用紧凑的分组侧边栏，支持中英双语内容搜索与键盘导航，搜索结果只会跳转到可见的控件。
- 设置界面整体焕新：间距更紧凑，图标底色改为渐变，页面与服务布局更紧凑。
### Fixed

- 全局快捷键现在可以在设置中清除，SpotAsk 不再占用系统热键。已安装的应用会保留当前快捷键，包括默认的 Option+Space。

## [0.2.2] - 2026-09-10

### Fixed

- 已结束并收起的思考过程不再让耗时计时持续刷新。
- 本地化不再在每次刷新思考过程标题时重新扫描语言目录。

## [0.2.1] - 2026-09-08

### Added

- 划词动作栏可以在提示词预设旁显示外部提问目标。两个独立开关可分别隐藏提示词或外部提问（默认都开启）。动作栏最多容纳八个动作。
- 可通过 `spotask://` URL 从 Alfred、Raycast、快捷指令或终端打开 SpotAsk，支持 `open`、`ask?q=`、`toggle` 和 `settings`。
- 新增 Homebrew Cask 分发：可直接通过 `brew install --cask spotask` 安装与更新 SpotAsk。

### Changed

- 全新安装时，「问 Grok」快捷操作默认关闭。

### Fixed

- 触发外部提问快捷操作后，输入框文字会被正确清空。
- 键盘导航与提交时会保留正在拼写的输入和多行文本。
- 关闭「窗口置顶」后，提问窗口在切换 Space 时不再跳到前台。
- 设置中的开关标签改用整行可用宽度，而不再受 134pt 控件列限制，较长的文案不再被截断。
- 划词助手设置页在控件放不下时会正常滚动。
- 带文字标签的划词动作栏提示词会保持在 400pt 面板内：过长标题会被截断，而不再渲染到画布之外。完整名称仍可在提示气泡中查看。
- 选区被清空、读取失败或关闭划词助手时，动作栏会一并消失并丢弃已捕获的选区快照。

## [0.2.0] - 2026-08-19

### Added

- 外部提问：在快速提问窗口中把问题发送到外部目标，随后窗口关闭并打开目标。内置「问 ChatGPT」与「问 Grok」，会在浏览器中打开并立即开始作答。
- 可添加自己的外部提问目标：打开网页链接、打开应用的链接，或在终端中执行 `omp {query}` 之类的命令。支持选择图标、调整顺序并分别启用。
- 设置中提供外部提问总开关，默认开启。
- 外部目标与自定义提示词预设支持品牌图标。
- 配置备份现在包含外部提问目标，并且兼容旧版备份。

### Fixed

- 本地重新安装后，划词助手会提示你重新允许当前安装读取选中文字。
- 快速提问窗口在一段时间无操作后会开始新对话。

## [0.1.6] - 2026-08-12

### Added

- 按模型配置思考等级：可关闭思考或选择具体等级，并支持服务商专属的请求接口兼容类型与自定义 JSON 请求参数。
- 启动时可选自动检查更新，发现新版本时在应用内提示，并可打开发布页面。
- 兼容性设置会根据服务与模型名称自动推断，同时保留你手动选择的配置。

### Fixed

- 在 Markdown 文本选择保持可用的同时，代码块复制按钮重新可以正常点击。

## [0.1.5] - 2026-08-11

### Added

- 模型选择器、对话标题栏和设置中会为受支持的服务显示品牌图标。
- 可选的 IM 式对话布局：模型消息在左、你的消息在右，以气泡呈现；可在「外观」设置中开启。
- 可从回答标题栏切换其他模型，并用它重新生成最新回答。
- 复制消息中选中的内容时，会还原标题、列表、引用、代码块和表格的 Markdown 标记。
- 回答内可渲染数学公式，并可在「外观」设置中开关。
- 新增中英双语的在线用户手册，可从「关于」设置中打开。

### Changed

- 划词助手自动触发会跳过空选区或仅含空白的选区。
- 已完成的回答中，代码块改为自动换行，不再打开独立的可滚动选择区域，因此拖拽选择可以连贯穿过。

### Fixed

- 消息文本可以跨多行连续选择并复制。
- 选中高亮在跨越 Markdown 块时保持连续，不再显示成多个分离的高亮片段。
- 焦点移开后，可用 ⌘+L 重新聚焦到问题输入框。
- 设置侧边栏中的项目获得焦点时，可用上下方向键在各分区之间切换。
- 在长时间深度思考的回答过程中，展开思考过程更加流畅。
- 普通点击或已经取消的选区不再触发划词助手的自动显示。

## [0.1.4] - 2026-08-07

### Added

- 快速切换模型：无需进入设置，直接在标题栏为当前对话选择任意服务的模型；开始新对话会回到默认模型。
- 临时附件：可粘贴截图、拖入图片或文本/代码文件，也可通过文件选择器添加；图片会作为图像内容发送，文本文件则作为文本上下文附加。
- 附件上下文会跟随对话：后续提问会重新发送此前的截图和文件，重试也会保留原有附件。
- 模型选择器支持搜索、按服务分组、键盘导航，并提供「使用默认模型」快捷项以回到设置中的默认模型。

- 跨应用划词助手：在 Safari、备忘录或其他应用中选中文字，即可翻译、解释、总结、润色或执行自定义提示词。
- 在选中文字旁显示快捷操作条，默认显示文字标签。
- 划词助手支持直接执行模式，并可选择自动触发。
- 划词助手的自动触发支持黑名单与白名单应用过滤，并提供可搜索的应用选择器。
- 除 OpenAI 兼容服务外，新增 Anthropic 服务支持，并支持模型发现与更新。
- 支持 HTTP 与 SOCKS5 代理，可选填账号密码，并可测试连接。
- 打开对话窗口的全局快捷键可自定义。
- 显示思考过程及耗时，并可选择默认展开思考过程。
- 每条回答提供包含复制与重试操作的操作栏。
- 界面语言可配置：English、简体中文、Español、Deutsch、日本語、Français、Português、Русский。
- 支持配置导出/导入，并可导出诊断日志用于排查问题。
- 使用紧凑的应用内提示条反馈操作结果。

### Changed

- 「总结」快捷操作改用更清晰的图标。
- 关闭划词助手后，相关设置项会折叠；仅在你启用该功能时才申请辅助功能权限。
- 对话角色使用图标标注：助手侧显示模型名称，你自己的消息显示「你」。
- 流式回答的更新只作用于当前消息，不再重写整个对话数组。
- 对话保留最近活跃部分为即时渲染，更早的历史仍然延迟加载。
- 流式回答以稳定的 Markdown 块渲染；只有活跃的尾部会重新解析，完成时会封存该尾部而不再替换整棵渲染树。
- 滚动几何回调仅在接近底部的状态真正变化时才更新跟随状态；用户滚动期间会禁用尺寸变化锚定。

### Fixed

- 回答仍在生成时，流式 Markdown 现在会保留段落、软换行、列表和代码块的换行。
- 在当前窗口中流式生成的长回答在完成后保持展开，已展开的助手消息在后续对话更新后也不会被收起。
- 对话与思考过程的滚动会把高频更新合并处理，不再为每次 token 刷新排队一条滚动指令。
- 输入框达到最大高度后，会跳过完整的 TextKit 测量。
- 部分构建中划词助手无法读取选中文字；现在在授予辅助功能权限后可稳定读取。
- 通过文本标记进行的选区现在可以正确解析。
- 关闭窗口后会保留输入框中的草稿，面板淡入淡出动画也已恢复。
- 深色模式下应用图标不再出现白色边角。
- 服务卡片可以稳定地展开和收起。
- 思考过程展开行为：开启时，思考期间保持展开，输出最终回答时收起；关闭时始终保持收起。

[Unreleased]: https://github.com/shiquda/SpotAsk/compare/v0.2.4...HEAD
[0.2.4]: https://github.com/shiquda/SpotAsk/compare/v0.2.3...v0.2.4
[0.2.3]: https://github.com/shiquda/SpotAsk/compare/v0.2.2...v0.2.3
[0.2.1]: https://github.com/shiquda/SpotAsk/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/shiquda/SpotAsk/compare/v0.1.6...v0.2.0
[0.1.6]: https://github.com/shiquda/SpotAsk/compare/v0.1.5...v0.1.6
[0.1.5]: https://github.com/shiquda/SpotAsk/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/shiquda/SpotAsk/compare/v0.1.3...v0.1.4
